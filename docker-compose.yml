version: "3"

services:
  db:
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: wizzit_pay
      POSTGRES_USER: wizzit_pay
      POSTGRES_DB: wizzit_pay
